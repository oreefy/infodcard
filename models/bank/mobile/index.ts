import { Create } from '@/models/bank/mobile/create';
import { Update } from '@/models/bank/mobile/update';
import { Delete } from '@/models/bank/mobile/delete';

const MobileModel = {
    create: Create,
    update: Update,
    delete: Delete
}

export default MobileModel;