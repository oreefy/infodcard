import { Create } from '@/models/bank/global/create';
import { Update } from '@/models/bank/global/update';
import { Delete } from '@/models/bank/global/delete';

const GlobalModel = {
    create: Create,
    update: Update,
    delete: Delete
}

export default GlobalModel;